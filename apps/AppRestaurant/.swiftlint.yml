# SwiftLint Configuration for AppRestaurant

disabled_rules:
  - trailing_whitespace

opt_in_rules:
  - empty_count
  - first_where
  - sorted_first_last

included:
  - AppRestaurant

excluded:
  - Pods
  - build
  - DerivedData

line_length:
  warning: 120
  error: 200

reporter: "xcode"
